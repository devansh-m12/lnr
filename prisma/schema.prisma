// This is a reference file. Use schema.prisma.generated for Prisma operations
// See separate files:
// - user.prisma
// - novel.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url = env("POSTGRES_PRISMA_URL")
  directUrl = env("POSTGRES_URL_NON_POOLING")
}